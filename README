TabSave is a safari extension that saves the URLs from all open tabs to an email, OmniFocus, or Instapaper.

To get the source run the following:
git clone https://github.com/dcandela/TabSave.git TabSave.safariextension

Add this as an extension from the Safari Extension Builder window found under the Developer menu item.
